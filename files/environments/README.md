# Environment directories
This is a environment directory. This means that certain packages work
inside this directory. Use ~direnv allow~ to enable the environment
directory. To have another ~.envrc~ file inside a subfolder include
~source_up~ in that file. See ~shell.nix~ file for environment
declaration.
